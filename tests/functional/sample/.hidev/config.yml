package:
    name:   test.dev

nginx:
    fpmSocket:  unix:/run/php/php7.0-fpm.sock
    test-a.dev:
        webDir:     /www/a.dev
    test-b.dev:
        webDir:     /www/b.dev
        ssl:        true
        sslPath:    /ssl/cert
